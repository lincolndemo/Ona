# Optional endpoint that receives lead capture POSTs:
#   { email, careerId, timestamp }
# If unset, leads are stored in localStorage instead. Email capture never blocks
# the result either way. Copy this file to .env.local and fill in a value to use.
VITE_LEAD_ENDPOINT=
